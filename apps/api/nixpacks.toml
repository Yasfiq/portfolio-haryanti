[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["cd ../.. && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd ../.. && pnpm --filter @repo/database build && pnpm --filter @portfolio/api build"]

[start]
cmd = "node dist/main.js"
